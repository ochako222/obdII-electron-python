{
  "name": "obd-electron-python",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "clean": "if [ -d dist ]; then rm -r dist; fi",
    "build": "tsc && copyfiles -u 1 src/*.html dist/",
    "start": "npm run clean && npm run build && electron ./dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "copyfiles": "^2.4.1",
    "electron": "^32.0.1",
    "typescript": "^5.5.4"
  }
}